# Abhängigkeiten (FreeRDP-Quellbaum, Linux-Build) – per scripts/build-freerdp-linux.sh erzeugt
/.deps/

# Build-Verzeichnisse
/build/
/build-*/

# FreeRDP und OpenSSL, statisch gebaut (scripts/build-freerdp-static.sh)
/vendor/

# Xcode, persönlich
xcuserdata/
*.xcuserstate
DerivedData/

# Prüf-Client-Ausgaben
/probe-out/

.DS_Store
/.env
